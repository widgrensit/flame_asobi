name: flame_asobi_example
description: Example multiplayer arena game using flame_asobi.
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ">=1.18.0 <2.0.0"
  flame_asobi:
    path: ../
